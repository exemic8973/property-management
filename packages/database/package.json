{
  "name": "@property-os/database",
  "version": "0.1.0",
  "private": true,
  "description": "PropertyOS Database - Prisma ORM",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "prisma generate && tsc",
    "generate": "prisma generate",
    "push": "prisma db push",
    "migrate": "prisma migrate dev",
    "studio": "prisma studio",
    "seed": "ts-node prisma/seed.ts",
    "clean": "rm -rf node_modules dist"
  },
  "dependencies": {
    "@nestjs/common": "^10.3.0",
    "@prisma/client": "^5.8.0"
  },
  "devDependencies": {
    "@types/node": "^20.19.31",
    "prisma": "^5.8.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
