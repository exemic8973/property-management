{
  "build_command": "cd ../.. && npm install -g pnpm && pnpm install && pnpm turbo run build --filter=@property-os/frontend",
  "start_command": "node server.js",
  "output_dir": ".next",
  "watch_paths": ["."]
}
